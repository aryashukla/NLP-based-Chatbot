NLP-based-Chatbot:

Trained from Cornell Movie-Dialogs Corpus. Used seq2seq method to train the RNN model.

Dataset is uploaded in cornell movie-dialogs corpus folder and code is in Main_code file

The code is upto preprocess the dataset and train the chatbot on that preprocesed data. To use the bot for chatting weights needs to be generated which can be generated after complete training.
